{
  "name": "create-workspace",
  "description": "A CLI tool to create a new workspace or update an existing one. Allows creating new web applications and documentation or manuscripts",
  "module": "index.ts",
  "type": "module",
  "version": "1.0.1",
  "bin": {
    "create-workspace": "index.ts",
    "cnw": "index.ts",
    "cnwx": "argvs.ts"
  },
  "scripts": {
    "dev": "bun run --watch index.ts",
    "pull": "bun run argvs.ts sync --restore",
    "prepush": "biome check --write && bun run argvs.ts sync",
    "create-link": "npm link;bun link;bun link create-workspace",
    "push": "bun run argvs.ts git"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "yaml": "^2.8.2"
  }
}
